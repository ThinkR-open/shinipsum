# A Random print output

This function returns a random print output that can be passed to
`renderPrint` and `verbatimTextOutput`.

## Usage

``` r
random_print(type = c("character", "numeric", "integer", "model", "table"))
```

## Arguments

- type:

  type of the output ("character", "numeric", "model", "table")

## Value

a random print
