# A Random ggplotly

This function returns a ggplotly object, which can be passed to
`renderPlotly` and `plotlyOutput`

## Usage

``` r
random_ggplotly(...)
```

## Arguments

- ...:

  arg to pass to `random_ggplot`.

## Value

a ggplotly
