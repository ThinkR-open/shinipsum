% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mock.R
\name{random_mock}
\alias{random_mock}
\title{A Mock UI Layout}
\usage{
random_mock(widths, heights = 3, labels = NULL)
}
\arguments{
\item{widths}{integer vector of bootstrap column widths (each between 1 and
12). Its length defines the number of mock zones.}

\item{heights}{integer vector giving the visual height of each zone, expressed
as a number of blank lines. Recycled to the length of \code{widths}. Default 3.}

\item{labels}{character vector of zone labels. Recycled to the length of
\code{widths}. When \code{NULL} (default), short random lorem-ipsum labels are used.}
}
\value{
a \code{\link[htmltools:tagList]{htmltools::tagList()}}
}
\description{
Build a quick "zoning" of a Shiny UI: a row of bootstrap columns, each one
drawn as an empty bordered box with a centered label. Handy to sketch the
layout of an app before any real content exists.
}
\details{
The returned object is a \code{\link[htmltools:tagList]{htmltools::tagList()}} (a self-contained CSS \verb{<style>}
block plus the columns), so it can be dropped directly inside a
\code{shiny::fluidPage()} / \code{bslib::page_fluid()} and rendered as is.
}
\examples{
random_mock(c(4, 8))
random_mock(c(6, 6), labels = c("a plot here", "a table here"))
}
