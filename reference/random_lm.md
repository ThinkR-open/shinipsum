# A Random Linear Model

This function returns a model which can be passed to renderText or
renderTable if pre-processed appropriately

## Usage

``` r
random_lm(nobs = 100, nx = 2)
```

## Arguments

- nobs:

  Numeric. number of observation

- nx:

  Numeric. number of variables. Should be lower that nobs

## Value

a model output
